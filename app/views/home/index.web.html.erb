<% content_for :feed_unread_count do %>
  (<%= @unread_count %>)
<% end %>

<% f = File.new("#{Rails.root}/vendor/weread.css") %>
<% content = f.read %>
<%= raw(content) %>

<script>
  var user_id = <%= current_user.id %>

    <% present('HomePresenter', current_user) do |rss| %>
  var rss_list_json = <%= raw(rss.rss_feeds_list(@rss_feeds_list)) %>
    <% end %>

    <% present('HomePresenter', current_user) do |rss| %>
    var all_rss_list_json = <%= raw(rss.all_rss_list_json) %>
    <% end %>

    var current_article = <%= raw(@rss_feeds_list.first.to_json) %>
</script>

<%= javascript_pack_tag 'web_app' %>

<div id="tianluo">
  <app></app>
</div>