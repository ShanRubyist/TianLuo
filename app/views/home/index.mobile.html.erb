<script>
    var user_id = <%= current_user.id %>

    <% present('RssFeedPresenter', user_id: current_user) do |rss| %>
    var rss_list_json = <%= raw(rss.rss_feed.to_json) %>
    <% end %>

    var unread_count_rss_feeds_path = '<%= unread_count_rss_feeds_path %>'

    var goods_data = <%= raw(@goods_list.to_json) %>
</script>

<%= javascript_pack_tag 'mobile_app' %>

<div id="tianluo">
  <app></app>
</div>
